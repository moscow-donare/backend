{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun src/index.ts",
    "migrate": "bun drizzle-kit migrate",
    "migrate:local": "DATABASE_URL=postgresql://donare:donare@localhost:5432/donare bun drizzle-kit generate && DATABASE_URL=postgresql://donare:donare@localhost:5432/donare bun drizzle-kit migrate",
    "migrate:docker": "DATABASE_URL=postgresql://donare:donare@postgres:5432/donare bun drizzle-kit generate && DATABASE_URL=postgresql://donare:donare@postgres:5432/donare bun drizzle-kit migrate",
    "migrate:docker:test": "DATABASE_URL=postgresql://postgres:postgres@postgrestest:5432/donare_test bun drizzle-kit generate && DATABASE_URL=postgresql://postgres:postgres@postgrestest:5432/donare_test bun drizzle-kit migrate",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.15.2",
    "@types/supertest": "^6.0.3",
    "drizzle-kit": "^0.31.1",
    "tsx": "^4.19.4",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "drizzle-orm": "^0.43.1",
    "ethers": "^6.14.4",
    "hono": "^4.7.10",
    "jose": "^6.0.11",
    "pg": "^8.16.0",
    "zod": "^3.25.63"
  }
}
